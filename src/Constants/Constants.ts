const genreText = 'genre';
const stateText = 'state';
const noResultsFound = 'No results were found.'
const defaultText = 'Default';
const inputPlaceHolder = 'Enter your search';
const allText = 'All';
const genreBigText = 'Genre';
const stateBigText = 'State';

export default {
    genreText,
    genreBigText,
    stateText,
    noResultsFound,
    defaultText,
    allText,
    inputPlaceHolder,
    stateBigText
}